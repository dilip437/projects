CREATE TABLE MEETING (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
  NAME VARCHAR(255),
  PRIMARY KEY (ID),
  UNIQUE KEY MEETING_NAME (NAME)
);


CREATE TABLE USERS_MEETINGS (
   USER_ID BIGINT NOT NULL,
   MEETING_ID BIGINT NOT NULL,
   PRIMARY KEY (USER_ID, MEETING_ID)
);

ALTER TABLE USERS_MEETINGS ADD CONSTRAINT USERS_MEETINGS_MEETING
   FOREIGN KEY (MEETING_ID) REFERENCES MEETING;

ALTER TABLE USERS_MEETINGS ADD CONSTRAINT USERS_MEETINGS_USER
   FOREIGN KEY (USER_ID) REFERENCES USER_;
   